// API Route: TourAPI areaBasedList1 프록시 (placeholder — Week 2)
import { NextResponse } from 'next/server';

export async function GET() {
  return NextResponse.json({ todo: 'Week 2 tourapi-integrator 구현 예정' }, { status: 501 });
}
