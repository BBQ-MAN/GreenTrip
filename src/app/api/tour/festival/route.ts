// API Route: TourAPI searchFestival1 프록시 (placeholder — Phase 2 W6~7)
import { NextResponse } from 'next/server';

export async function GET() {
  return NextResponse.json({ todo: 'Phase 2 W6~7 구현 예정' }, { status: 501 });
}
